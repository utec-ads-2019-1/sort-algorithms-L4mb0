//
// Created by root on 4/2/19.
//
#include <iostream>
#include <thread>
#include <string>
#include <vector>
#include <map>
#include <set>
#include <limits>
#include <queue>
#include <cmath>
#include <fstream>
#include <algorithm>
#include<bits/stdc++.h>
#include <future>
#include <array>

#define FOR(m) for(int i=0;i<m;i++)
#define FOR2(m) for(int j=0; j<m; j++)
#define ll long long int
#define scl1(a)             scanf("%lld", &a)
#define REP(i, n) FOR(i,0,n)
#define pb(x) push_back(x)
#define fst first
#define snd second
#define foritpnt for(auto it=datos->begin(); it != datos->end(); it++)
#define forit for(auto it=datos.begin(); it != datos.end(); it++)
#define sz(x) int(x.size())
#define all(v) v.begin(),v.end()
#define mp(x, y) make_pair(x,y)
#define fastcin std::ios::sync_with_stdio(false); std::cin.tie(nullptr);

#ifndef SORT_ALGORITHMS_L4MB0_INCLUDESS_H
#define SORT_ALGORITHMS_L4MB0_INCLUDESS_H

#endif //SORT_ALGORITHMS_L4MB0_INCLUDESS_H
